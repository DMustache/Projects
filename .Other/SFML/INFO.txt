http://izformatika.ru/mod/quiz/review.php?attempt=8385&cmid=1694
3 и 5 вопросы отмечы неверными, хотя по идее я их решил верно
 
#include <iostream>
#include <vector>
#include <cmath>
using namespace std;

int main()
{
int qtty=5, tmp(0), tmp1(0), tmp2(0), mif(10001), summax(0), cif;
for(int i=0; i
{
for(int j; j
cin»tmp»tmp1»tmp2;
summax=summax+max(max(tmp, tmp1), tmp2);
cif=abs(tmp-tmp1);
if((cif%3!=0) && (cif
mif=cif;
}
if(summax%3!=0)
cout<
else if ((summax%3!=0)&&(summax>mif))
cout<
else
cout«"Save me!";
}